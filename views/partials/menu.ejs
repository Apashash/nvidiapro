<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<%- include('language') %>
<style>
    :root {
        --amber: #D60000;
        --amber-dark: #B90000;
        --nav-bg: #FFFFFF;
        --nav-inactive: #77777C;
        --nav-active: #D60000;
        --nav-border: #14105F;
    }
    .bottom-nav {
        position: fixed;
        bottom: 0;
        left: 0;
        width: 100%;
        background: var(--nav-bg);
        border-top: 1px solid var(--nav-border);
        display: flex;
        justify-content: space-around;
        align-items: center;
        padding: 8px 0 12px;
        z-index: 9999;
        box-shadow: 0 -4px 20px rgba(20,16,95,0.12);
    }
    .nav-item {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        color: var(--nav-inactive);
        text-decoration: none;
        font-size: 10px;
        padding: 4px 0;
        font-family: 'Montserrat', Arial, sans-serif;
        font-weight: 500;
        transition: color 0.2s;
        gap: 3px;
    }
    .nav-item i { font-size: 20px; }
    .nav-item span { font-size: 10px; letter-spacing: 0.2px; }
    .nav-item.active { color: var(--nav-active); font-weight: 700; }
    .nav-center {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-decoration: none;
        gap: 3px;
        margin-top: -22px;
    }
    .nav-center-btn {
        width: 54px;
        height: 54px;
        border-radius: 50%;
        background: #14105F;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 4px 16px rgba(20,16,95,0.35);
        border: 3px solid #fff;
    }
    .nav-center-btn i { font-size: 22px; color: #fff; }
    .nav-center span { font-size: 10px; color: var(--nav-inactive); font-weight: 500; font-family: 'Montserrat', Arial, sans-serif; }
    .nav-center.active .nav-center-btn { background: #14105F; }
    .nav-center.active span { color: var(--nav-active); font-weight: 700; }
    body { padding-bottom: 80px !important; }
</style>
<%
    const configuredCurrencyRate = Number(locals.appParams && appParams.taux_usdt_fcfa);
    const currencyRate = Number.isFinite(configuredCurrencyRate) && configuredCurrencyRate > 0
        ? configuredCurrencyRate
        : 600;
%>
<script>window.GD_USDT_FCFA_RATE = <%= currencyRate %>;</script>
<script src="/assets/currency-display.js"></script>

<nav class="bottom-nav">
    <a href="/" class="nav-item <%= locals.page === 'index' ? 'active' : '' %>">
        <i class="fas fa-home"></i><span>Accueil</span>
    </a>
    <a href="/investissement" class="nav-item <%= locals.page === 'investissement' ? 'active' : '' %>">
        <i class="fas fa-chart-bar"></i><span>Investir</span>
    </a>
    <a href="/salaire" class="nav-center <%= locals.page === 'salaire' ? 'active' : '' %>">
        <div class="nav-center-btn"><i class="fas fa-coins"></i></div>
        <span>Salaire</span>
    </a>
    <a href="/equipe" class="nav-item <%= locals.page === 'equipe' ? 'active' : '' %>">
        <i class="fas fa-users"></i><span>Équipe</span>
    </a>
    <a href="/compte" class="nav-item <%= locals.page === 'compte' ? 'active' : '' %>">
        <i class="fas fa-user"></i><span>Profil</span>
    </a>
</nav>

<% if (locals.appParams && appParams.whatsapp_link) { %>
<a href="<%= appParams.whatsapp_link %>" target="_blank" rel="noopener"
   style="position:fixed;bottom:100px;right:16px;width:52px;height:52px;border-radius:50%;
          background:#25D366;color:#fff;display:flex;align-items:center;justify-content:center;
          box-shadow:0 6px 20px rgba(37,211,102,0.5);z-index:9998;text-decoration:none;
          transition:transform 0.2s;" onmouseover="this.style.transform='scale(1.1)'" onmouseout="this.style.transform='scale(1)'">
  <i class="fab fa-whatsapp" style="font-size:26px;"></i>
</a>
<% } %>
